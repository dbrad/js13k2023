// script that:
// opens the VERSION file
// parse the line
// increase the selected number, reset all below
// keep this value in a variable
// save the VERSION file
// open the package.json
// replace the version value with the new value
// save the package.json

// Major    == Breaking Release
// Minor    == Non-breaking features / additions
// Revision == Bug fixes
// Build    == WIP / Testing Prior to next release

console.log("bump");